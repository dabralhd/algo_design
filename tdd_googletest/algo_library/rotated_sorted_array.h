/*
 * rotated_sorted_array.cpp
 *
 *  Created on: 20-Jan-2023
 *      Author: hem
 */

#ifndef INC_ROTATED_SORTED_ARRAY_CPP_
#define INC_ROTATED_SORTED_ARRAY_CPP_

#include <vector>

using namespace std;

/*
 * finds the index of pivot (smallest element) in a rotated sorted array
 * nums [input] rotated sorted array
 */
int get_pivot(vector<int> nums);

#endif /* INC_ROTATED_SORTED_ARRAY_CPP_ */
